{
  "name": "onyx-terminal",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A terminal-style, privacy-focused YouTube frontend powered by the Invidious API.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js",
    "test": "node --test"
  }
}
